---
import DirectoryLayout from "../../layouts/directoryLayout.astro";
import { members_api_url } from "../../data/conf.json";

const response = await fetch(members_api_url + "members-data");
const members_data = await response.json();
const members_array = Object.values(members_data);

---

<DirectoryLayout>
  <div class="grid grid-cols-3 gap-6">
    {
      members_array.map((member) => (
        <div class="w-full max-w-sm bg-white border border-gray-200 rounded-lg dark:bg-gray-800 dark:border-gray-700">
          <a href={"/dev/" + member["username"]} class="flex flex-col items-center py-5">
            <img
              class="w-24 h-24 my-3 rounded-full shadow-lg"
              src={member["avatar_url"]}
              alt={member["name"] + " profile picture"}
            />
            <h5 class="mb-1 text-xl font-medium text-gray-900 dark:text-white">{member["name"]}</h5>
            <span class="text-sm text-gray-500 dark:text-gray-400 border px-3 py-0.5 rounded-full">
              {member["headline"]}
            </span>
            <div class="flex mt-2 line-clamp-2 overflow-hidden text-sm font-normal text-gray-600 space-x-3 px-5">
              {member["bio"]}
            </div>
            <div class="my-3 px-5">
              {member["roles"].map((role) => (
                <div class="inline rounded-md bg-gray-50 text-gray-800 font-normal text-sm py-1 px-2 mr-3 ">{role}</div>
              ))}
            </div>
          </a>
        </div>
      ))
    }
  </div>
</DirectoryLayout>
