---
const { img, description, name, url } = Astro.props;
import { Image } from "@astrojs/image/components";
---

<a href={url} class="flex space-x-4 my-4">
  <Image
    class="w-10 h-10 rounded-md"
    src={img}
    alt={name + " logo"}
    format="webp"
    quality={85}
    width={40}
    height={40}
  />
  <div class="font-medium dark:text-white">
    <div class="text-gray-700 dark:text-gray-100 font-medium">{name}</div>
    <div class="text-sm font-normal text-gray-500 dark:text-white line-clamp-2">{description}</div>
  </div>
</a>
